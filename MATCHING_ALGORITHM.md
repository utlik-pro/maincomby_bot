# 🎯 Алгоритм работы системы матчинга

## 📊 Визуальная схема работы

```
┌─────────────────────────────────────────────────────────────────┐
│                    ПОЛЬЗОВАТЕЛЬ НАЧИНАЕТ СВАЙПИНГ                │
│                      /tinder → "🔥 Начать свайпить"              │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│              ШАГ 1: СБОР ДАННЫХ О ПОЛЬЗОВАТЕЛЕ                   │
├─────────────────────────────────────────────────────────────────┤
│  1. Получаем город пользователя из его профиля                   │
│     profile_city = user.profile.city  (например "Минск")         │
│                                                                  │
│  2. Получаем список УЖЕ ПРОСМОТРЕННЫХ профилей                   │
│     SELECT swiped_id FROM swipes WHERE swiper_id = user.id       │
│     swiped_ids = [3, 5, 7, 12, 15] ← уже видел эти профили      │
│                                                                  │
│  3. Получаем мероприятия, на которые зарегистрирован             │
│     SELECT event_id FROM event_registrations                     │
│     WHERE user_id = user.id                                      │
│     user_event_ids = [1, 3] ← зарегистрирован на события 1 и 3  │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│              ШАГ 2: ПОИСК ПРОФИЛЯ С ПРИОРИТЕТАМИ                 │
└─────────────────────────────────────────────────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐    ┌───────────────┐    ┌──────────────────┐
│  ПРИОРИТЕТ 1  │    │  ПРИОРИТЕТ 2  │    │   ПРИОРИТЕТ 3    │
│               │    │               │    │                  │
│ Общие события │    │  Тот же город │    │ Все остальные    │
│  + тот же     │    │               │    │  одобренные      │
│     город     │    │               │    │                  │
└───────┬───────┘    └───────┬───────┘    └────────┬─────────┘
        │                    │                     │
        │ Есть профиль?      │ Есть профиль?       │ Есть профиль?
        │                    │                     │
        │ ДА ──────┐         │ ДА ──────┐          │ ДА ──────┐
        │          │         │          │          │          │
        │ НЕТ      │         │ НЕТ      │          │ НЕТ      │
        │          │         │          │          │          │
        └──────────┼─────────┴──────────┼──────────┴──────────┼───────┐
                   │                    │                     │       │
                   └────────────────────┴─────────────────────┘       │
                                        │                             │
                                        ▼                             ▼
                          ┌──────────────────────┐      ┌────────────────────┐
                          │  ПОКАЗАТЬ КАРТОЧКУ   │      │  НЕТ ПРОФИЛЕЙ      │
                          │     ПРОФИЛЯ          │      │                    │
                          └──────────┬───────────┘      │ "Вы посмотрели все │
                                     │                  │ доступные профили" │
                                     ▼                  └────────────────────┘
┌─────────────────────────────────────────────────────────────────┐
│                  ШАГ 3: ОТОБРАЖЕНИЕ КАРТОЧКИ                     │
├─────────────────────────────────────────────────────────────────┤
│  Карточка профиля:                                               │
│  ┌─────────────────────────────────────────┐                    │
│  │  [📷 Фото если есть]                     │                    │
│  │                                          │                    │
│  │  👤 Иван Петров                          │                    │
│  │  🏙 Минск                                │                    │
│  │                                          │                    │
│  │  📝 Fullstack разработчик, люблю Python  │                    │
│  │      и кофе. Ищу партнера для стартапа   │                    │
│  │                                          │                    │
│  │  💼 Занимается: Backend developer        │                    │
│  │                                          │                    │
│  │  [❤️ Лайк]  [👎 Скип]                   │                    │
│  │  [👀 Подробнее]                          │                    │
│  └─────────────────────────────────────────┘                    │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                   ┌───────────┴───────────┐
                   │                       │
                   ▼                       ▼
         ┌─────────────────┐    ┌─────────────────┐
         │  ПОЛЬЗОВАТЕЛЬ   │    │  ПОЛЬЗОВАТЕЛЬ   │
         │  НАЖАЛ "❤️ ЛАЙК"│    │  НАЖАЛ "👎 СКИП"│
         └────────┬────────┘    └────────┬────────┘
                  │                      │
                  ▼                      ▼
┌─────────────────────────────────────────────────────────────────┐
│            ШАГ 4: ОБРАБОТКА ДЕЙСТВИЯ ПОЛЬЗОВАТЕЛЯ                │
├─────────────────────────────────────────────────────────────────┤
│  1. Записываем действие в базу:                                  │
│     INSERT INTO swipes                                           │
│     (swiper_id, swiped_id, action, swiped_at)                   │
│     VALUES (current_user_id, target_user_id, 'like', NOW())     │
│                                                                  │
│  2. ЕСЛИ действие = "skip":                                      │
│     → Показать следующий профиль (вернуться к ШАГ 2)            │
│                                                                  │
│  3. ЕСЛИ действие = "like":                                      │
│     → Проверить взаимность                                       │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│              ШАГ 5: ПРОВЕРКА ВЗАИМНОСТИ ЛАЙКА                    │
├─────────────────────────────────────────────────────────────────┤
│  SQL запрос:                                                     │
│  SELECT * FROM swipes                                            │
│  WHERE swiper_id = target_user_id                               │
│    AND swiped_id = current_user_id                              │
│    AND action = 'like'                                          │
│                                                                  │
│  Вопрос: Лайкнул ли нас целевой пользователь раньше?            │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                ┌──────────────┴──────────────┐
                │                             │
                ▼                             ▼
    ┌──────────────────┐          ┌─────────────────────┐
    │   НЕТ ВЗАИМНОГО  │          │  ЕСТЬ ВЗАИМНЫЙ ЛАЙК │
    │      ЛАЙКА       │          │    (mutual_like)    │
    └────────┬─────────┘          └──────────┬──────────┘
             │                               │
             │                               ▼
             │              ┌─────────────────────────────────────┐
             │              │     ШАГ 6: СОЗДАНИЕ МАТЧА           │
             │              ├─────────────────────────────────────┤
             │              │ 1. Создаём запись в таблице matches:│
             │              │    INSERT INTO matches              │
             │              │    (user1_id, user2_id, matched_at) │
             │              │    VALUES (min_id, max_id, NOW())   │
             │              │                                     │
             │              │ 2. Получаем профили обоих:          │
             │              │    profile1 = get_profile(user1_id) │
             │              │    profile2 = get_profile(user2_id) │
             │              │                                     │
             │              │ 3. Отправляем уведомления:          │
             │              │    → Пользователю 1                 │
             │              │    → Пользователю 2                 │
             │              └──────────┬──────────────────────────┘
             │                         │
             │                         ▼
             │              ┌─────────────────────────────────────┐
             │              │   ШАГ 7: УВЕДОМЛЕНИЯ О МАТЧЕ        │
             │              ├─────────────────────────────────────┤
             │              │ Каждому пользователю отправляется:  │
             │              │                                     │
             │              │ 💕 У ВАС НОВЫЙ МАТЧ!                │
             │              │                                     │
             │              │ 👤 Имя: Иван Петров                 │
             │              │ 💼 Чем занимается: ...              │
             │              │ 🔍 Ищет: ...                        │
             │              │ 🤝 Может помочь: ...                │
             │              │ 🆘 Нуждается: ...                   │
             │              │ 🏙 Город: Минск                     │
             │              │                                     │
             │              │ 📞 Контакты:                        │
             │              │   • Telegram: @username             │
             │              │   • Телефон: +375291234567          │
             │              │                                     │
             │              │ Можете написать и познакомиться! 🎉 │
             │              └──────────┬──────────────────────────┘
             │                         │
             │                         │
             └─────────────────────────┴────────────────┐
                                                        │
                                                        ▼
                                        ┌───────────────────────────┐
                                        │  ПОКАЗАТЬ СЛЕДУЮЩИЙ       │
                                        │  ПРОФИЛЬ                  │
                                        │  (вернуться к ШАГ 2)      │
                                        └───────────────────────────┘
```

---

## 🔍 Детальное описание каждого приоритета

### ПРИОРИТЕТ 1: Общие мероприятия + тот же город

**Почему это важно?** Если вы оба посещаете одно мероприятие, вы можете встретиться там лично!

**SQL запрос:**
```sql
SELECT DISTINCT p.*
FROM user_profiles p
JOIN users u ON p.user_id = u.id
JOIN event_registrations er ON er.user_id = u.id
WHERE
  p.user_id != current_user_id                    -- Не показывать себя
  AND p.moderation_status = 'approved'            -- Только одобренные
  AND p.is_visible = TRUE                         -- Только видимые
  AND p.city = 'Минск'                            -- Тот же город
  AND er.event_id IN (1, 3)                       -- Общие мероприятия
  AND p.user_id NOT IN (3, 5, 7, 12, 15)          -- Исключить просмотренных
LIMIT 1
```

**Пример:**
```
Вы: Минск, зарегистрированы на "Python Meetup" (event_id=1)
Показывается: Иван из Минска, тоже на "Python Meetup"
```

### ПРИОРИТЕТ 2: Тот же город

**Почему это важно?** Легче встретиться оффлайн, общие локальные интересы.

**SQL запрос:**
```sql
SELECT p.*
FROM user_profiles p
WHERE
  p.user_id != current_user_id
  AND p.moderation_status = 'approved'
  AND p.is_visible = TRUE
  AND p.city = 'Минск'                            -- Тот же город
  AND p.user_id NOT IN (3, 5, 7, 12, 15)
LIMIT 1
```

**Пример:**
```
Вы: Минск
Показывается: Мария из Минска (даже если нет общих мероприятий)
```

### ПРИОРИТЕТ 3: Все остальные одобренные профили

**Почему это важно?** Расширяем круг знакомств за пределы города.

**SQL запрос:**
```sql
SELECT p.*
FROM user_profiles p
WHERE
  p.user_id != current_user_id
  AND p.moderation_status = 'approved'
  AND p.is_visible = TRUE
  AND p.user_id NOT IN (3, 5, 7, 12, 15)
LIMIT 1
```

**Пример:**
```
Вы: Минск
Показывается: Петр из Гродно (другой город, но тоже в сообществе)
```

---

## 💾 Структура данных в базе

### Таблица `swipes` - История всех действий

```
+----+-----------+-----------+--------+---------------------+
| id | swiper_id | swiped_id | action | swiped_at           |
+----+-----------+-----------+--------+---------------------+
| 1  | 5         | 3         | like   | 2024-11-24 10:00:00 |
| 2  | 5         | 7         | skip   | 2024-11-24 10:01:00 |
| 3  | 3         | 5         | like   | 2024-11-24 10:05:00 | ← ВЗАИМНЫЙ!
| 4  | 5         | 12        | like   | 2024-11-24 10:10:00 |
+----+-----------+-----------+--------+---------------------+
```

**Объяснение:**
- Строка 1: Пользователь 5 лайкнул пользователя 3
- Строка 2: Пользователь 5 скипнул пользователя 7
- Строка 3: Пользователь 3 лайкнул пользователя 5 → **МАТЧ!** (т.к. строка 1 существует)
- Строка 4: Пользователь 5 лайкнул пользователя 12 (пока без ответного лайка)

### Таблица `matches` - Взаимные матчи

```
+----+----------+----------+---------------------+-----------+
| id | user1_id | user2_id | matched_at          | is_active |
+----+----------+----------+---------------------+-----------+
| 1  | 3        | 5        | 2024-11-24 10:05:00 | 1         |
+----+----------+----------+---------------------+-----------+
```

**Важно:**
- `user1_id` всегда меньше `user2_id` (используется `min(id1, id2)` и `max(id1, id2)`)
- Это предотвращает дубликаты: (3,5) и (5,3) - это один матч

---

## 🔄 Пошаговый пример: Как создаётся матч

### Сценарий: Анна и Борис

**Исходное состояние базы:**

`users`:
```
id | tg_user_id | username | first_name
---+------------+----------+-----------
10 | 111111     | anna_dev | Анна
20 | 222222     | boris_pm | Борис
```

`user_profiles`:
```
id | user_id | bio                  | city  | moderation_status
---+---------+----------------------+-------+------------------
1  | 10      | Frontend developer   | Минск | approved
2  | 20      | Product manager      | Минск | approved
```

### Шаг 1: Анна видит профиль Бориса и ставит лайк

**Действие:** Анна нажимает "❤️ Лайк" на профиле Бориса

**В базу добавляется запись:**
```sql
INSERT INTO swipes (swiper_id, swiped_id, action)
VALUES (10, 20, 'like');
```

**Проверка взаимности:**
```sql
SELECT * FROM swipes
WHERE swiper_id = 20 AND swiped_id = 10 AND action = 'like';
-- Результат: пусто (Борис ещё не видел Анну)
```

**Результат:** Лайк записан, но матча нет. Анна продолжает свайпить.

### Шаг 2: Борис видит профиль Анны и ставит лайк

**Действие:** Борис нажимает "❤️ Лайк" на профиле Анны

**В базу добавляется запись:**
```sql
INSERT INTO swipes (swiper_id, swiped_id, action)
VALUES (20, 10, 'like');
```

**Проверка взаимности:**
```sql
SELECT * FROM swipes
WHERE swiper_id = 10 AND swiped_id = 20 AND action = 'like';
-- Результат: НАЙДЕНА запись из Шага 1!
```

**✅ ВЗАИМНЫЙ ЛАЙК ОБНАРУЖЕН!**

**Создаётся матч:**
```sql
INSERT INTO matches (user1_id, user2_id, matched_at)
VALUES (10, 20, NOW());  -- min(10,20) = 10, max(10,20) = 20
```

**Отправляются уведомления:**

**Анне приходит:**
```
💕 У ВАС НОВЫЙ МАТЧ!

👤 Имя: Борис
💼 Чем занимается: Product manager
🔍 Ищет: команду для стартапа
🤝 Может помочь: стратегия продукта
🏙 Город: Минск

📞 Контакты:
  • Telegram: @boris_pm
  • Телефон: +375291234567

Можете написать и познакомиться! 🎉
```

**Борису приходит:**
```
💕 У ВАС НОВЫЙ МАТЧ!

👤 Имя: Анна
💼 Чем занимается: Frontend developer
🔍 Ищет: интересные проекты
🤝 Может помочь: разработка интерфейсов
🏙 Город: Минск

📞 Контакты:
  • Telegram: @anna_dev
  • Телефон: +375339876543

Можете написать и познакомиться! 🎉
```

**Борису также показывается всплывающее уведомление:**
```
💕 Это матч!
```

---

## 📈 Статистика и аналитика

### Match Rate (Процент успешных матчей)

**Формула:**
```
Match Rate = (Количество матчей × 2 / Количество лайков) × 100%
```

**Пример:**
```
Матчей: 10
Лайков: 50

Match Rate = (10 × 2 / 50) × 100% = 40%
```

**Почему умножаем на 2?**
Каждый матч = 2 лайка (один от каждого пользователя)

### Пример расчёта в коде

```python
matches_count = 10  # Из таблицы matches
likes_count = 50    # Из таблицы swipes (action='like')

if likes_count > 0:
    match_rate = (matches_count * 2 / likes_count * 100)
    print(f"Match rate: {match_rate:.1f}%")  # → "Match rate: 40.0%"
```

---

## 🎨 Визуализация алгоритма приоритетов

```
База данных содержит 10 одобренных профилей:

Профиль  | Город  | Общие события | Статус
---------|--------|---------------|------------------
User 2   | Минск  | ✅ Да (1,3)   | ← ПОКАЗАТЬ ПЕРВЫМ (Приоритет 1)
User 4   | Минск  | ✅ Да (1)     | ← ПОКАЗАТЬ ВТОРЫМ (Приоритет 1)
User 7   | Минск  | ❌ Нет        | ← ПОКАЗАТЬ ТРЕТЬИМ (Приоритет 2)
User 9   | Минск  | ❌ Нет        | ← ПОКАЗАТЬ ЧЕТВЁРТЫМ (Приоритет 2)
User 12  | Гродно | ❌ Нет        | ← ПОКАЗАТЬ ПЯТЫМ (Приоритет 3)
User 15  | Гродно | ❌ Нет        | ← ПОКАЗАТЬ ШЕСТЫМ (Приоритет 3)
```

Система автоматически сортирует и показывает профили в этом порядке!

---

## 🛡️ Фильтры безопасности

Профиль **НЕ ПОКАЗЫВАЕТСЯ**, если:

❌ `moderation_status != 'approved'` (не одобрен админом)
❌ `is_visible = FALSE` (пользователь скрыл профиль)
❌ `user_id = current_user_id` (это ваш собственный профиль)
❌ `user_id IN (swiped_ids)` (вы уже видели этот профиль)

---

## 💡 Оптимизация производительности

### Индексы в базе данных

```sql
-- Быстрый поиск свайпов
CREATE INDEX ix_swipes_swiper_id ON swipes (swiper_id);
CREATE INDEX ix_swipes_swiped_id ON swipes (swiped_id);
CREATE INDEX ix_swipes_action ON swipes (action);

-- Быстрый поиск профилей
CREATE INDEX ix_user_profiles_city ON user_profiles (city);
CREATE INDEX ix_user_profiles_moderation_status ON user_profiles (moderation_status);
CREATE INDEX ix_user_profiles_is_visible ON user_profiles (is_visible);

-- Быстрый поиск матчей
CREATE INDEX ix_matches_user1_id ON matches (user1_id);
CREATE INDEX ix_matches_user2_id ON matches (user2_id);
CREATE INDEX ix_matches_is_active ON matches (is_active);
```

### DISTINCT в запросах

При поиске по общим мероприятиям используется `DISTINCT`, чтобы один пользователь не показывался несколько раз, если у вас несколько общих событий.

---

**Документация обновлена:** 24 ноября 2024
**Автор:** Claude Code
