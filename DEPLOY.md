# 🚀 Деплой Telegram бота на Dokploy

## Предварительные требования

1. **VPS сервер** с установленным Dokploy
2. **GitHub репозиторий** с кодом бота
3. **Telegram Bot Token** (от @BotFather)

---

## 📋 Шаг 1: Подготовка проекта

Убедитесь, что у вас есть все необходимые файлы:
- ✅ `Dockerfile`
- ✅ `docker-compose.yml`
- ✅ `.dockerignore`
- ✅ `requirements.txt`
- ✅ `.env.example`

---

## 🔧 Шаг 2: Настройка Dokploy

### 2.1. Создание нового приложения

1. Откройте Dokploy UI (обычно `http://ваш-сервер:3000`)
2. Создайте новый проект: **"New Project"**
3. Выберите **"Application"** → **"Docker Compose"**

### 2.2. Подключение GitHub репозитория

1. В разделе **"Git Repository"**:
   - Repository URL: `https://github.com/ваш-username/maincomby_bot`
   - Branch: `main` или `master`
   - Auto Deploy: ✅ (автоматический деплой при push)

### 2.3. Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

```bash
# Обязательные переменные
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=123456789,987654321

# База данных (SQLite в контейнере)
DATABASE_URL=sqlite+aiosqlite:///./data/bot.db

# Опциональные переменные
OPENAI_API_KEY=ваш_ключ_openai
TIMEZONE=Europe/Minsk

# ID чатов (если используете)
INTERMEDIATE_CHAT_ID=-1001234567890
TARGET_CHANNEL_ID=-1001234567890
WELCOME_CHAT_ID=-1001234567890
WELCOME_THREAD_ID=12345
```

### 2.4. Настройка Volume (для сохранения БД)

В разделе **"Volumes"**:
- Container Path: `/app/data`
- Host Path: `/data/maincomby_bot`
- ✅ Persistent

---

## 🎯 Шаг 3: Деплой

1. Нажмите **"Deploy"**
2. Dokploy автоматически:
   - Склонирует репозиторий
   - Соберет Docker образ
   - Запустит контейнер
3. Проверьте логи: **"Logs"** → убедитесь что бот запустился

---

## ✅ Шаг 4: Проверка

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должно появиться сообщение о мероприятии

---

## 🔄 Обновление бота

### Автоматическое обновление (рекомендуется)
1. Просто сделайте `git push` в репозиторий
2. Dokploy автоматически пересоберет и перезапустит бот

### Ручное обновление
1. В Dokploy UI → ваше приложение
2. Нажмите **"Redeploy"**

---

## 📊 Мониторинг

### Просмотр логов
```bash
# В Dokploy UI
Applications → maincomby_bot → Logs
```

### Просмотр статуса
```bash
# В Dokploy UI
Applications → maincomby_bot → Overview
```

### Подключение к контейнеру (для отладки)
```bash
# SSH на сервер
ssh user@ваш-сервер

# Найти контейнер
docker ps | grep maincomby_bot

# Подключиться
docker exec -it maincomby_bot /bin/bash

# Проверить БД
sqlite3 /app/data/bot.db "SELECT * FROM events;"
```

---

## 🗄️ Резервное копирование БД

### Автоматический бэкап (рекомендуется)

Создайте cron задачу на сервере:
```bash
# Редактировать crontab
crontab -e

# Добавить строку (бэкап каждый день в 3:00)
0 3 * * * docker exec maincomby_bot sqlite3 /app/data/bot.db ".backup '/app/data/backup_$(date +\%Y\%m\%d).db'"
```

### Ручной бэкап
```bash
# SSH на сервер
docker exec maincomby_bot sqlite3 /app/data/bot.db ".backup '/app/data/backup.db'"

# Скачать бэкап
scp user@сервер:/data/maincomby_bot/backup.db ./
```

---

## 🐛 Решение проблем

### Бот не запускается
1. Проверьте логи в Dokploy
2. Убедитесь что все переменные окружения заданы
3. Проверьте что BOT_TOKEN корректный

### База данных пропала после перезапуска
1. Убедитесь что Volume настроен правильно
2. Проверьте что путь `/app/data` смонтирован

### Конфликт портов
Telegram бот не использует порты - только исходящие соединения

---

## 📝 Полезные команды

### Перезапуск бота
```bash
# В Dokploy UI
Applications → maincomby_bot → Restart
```

### Просмотр всех логов
```bash
docker logs -f maincomby_bot
```

### Проверка использования ресурсов
```bash
docker stats maincomby_bot
```

---

## 🔐 Безопасность

1. **Не коммитьте .env файл** в Git
2. **Используйте секреты** в Dokploy для токенов
3. **Регулярно обновляйте** зависимости
4. **Делайте бэкапы** базы данных

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в Dokploy
2. Проверьте документацию Dokploy
3. Проверьте issues в GitHub репозитории

---

## 🎉 Готово!

Ваш бот теперь работает 24/7 на Dokploy! 🚀
