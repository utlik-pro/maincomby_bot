# 📤 Как работает Git Push (БЕЗ SSH к серверу)

## 🎯 Важное уточнение

Когда мы говорим **"обновление бота БЕЗ SSH"**, речь идёт о том, что **не нужно подключаться к серверу vdska.ru через SSH** для каждого обновления.

## 📊 Схема работы

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   Локально  │  HTTPS  │   GitHub    │  Auto   │  vdska.ru   │
│   (твой ПК) │ ──────> │  (репо)     │ ──────> │  (сервер)   │
└─────────────┘         └─────────────┘         └─────────────┘
  git push                репозиторий            бот обновляется
  (НЕ SSH!)               обновился              автоматически
```

### Детально:

1. **Локально → GitHub** (git push)
   - Используется **HTTPS** или **SSH ключи GitHub** (не путать с SSH к серверу!)
   - Это стандартный git push в GitHub репозиторий
   - **Пароль НЕ нужен**, если настроен токен или SSH ключи GitHub

2. **GitHub → vdska.ru** (автоматически)
   - Сервер проверяет GitHub каждые 5 минут (Cron)
   - Или получает webhook от GitHub (30 секунд)
   - Автоматически делает `git pull` и перезапускает бота
   - **SSH к серверу НЕ используется**

---

## 🔐 Два вида SSH (не путать!)

### 1. SSH к серверу vdska.ru ❌ (НЕ нужен после настройки)

```bash
# Это НЕ нужно делать каждый раз:
ssh root@104.253.1.54
cd /root/maincomby_bot
git pull
docker compose restart
# ❌ Слишком много действий!
```

**Используется только 1 раз** для начальной настройки автодеплоя.

### 2. SSH для GitHub ✅ (опционально, для git push)

```bash
# Если хочешь использовать SSH вместо HTTPS для GitHub:
git remote set-url origin git@github.com:username/maincomby_bot.git
git push origin main
# ✅ Это SSH к GitHub, а не к vdska.ru!
```

Но можно использовать и **HTTPS с токеном**:

```bash
git remote set-url origin https://github.com/username/maincomby_bot.git
git push origin main
# Вводишь GitHub username + personal access token
```

---

## 🚀 Как это работает на практике

### Вариант 1: HTTPS (проще)

```bash
# Настройка один раз:
git remote -v
# origin  https://github.com/username/maincomby_bot.git

# При первом push попросит авторизацию:
git push origin main
# Username: твой_github_username
# Password: ghp_твой_personal_access_token
# (токен создаётся на github.com/settings/tokens)

# Дальше можно сохранить токен в credential manager
git config --global credential.helper store
# Теперь не будет спрашивать каждый раз
```

### Вариант 2: SSH к GitHub (безопаснее)

```bash
# Настройка один раз:
# 1. Генерируем SSH ключ (если нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. Добавляем публичный ключ на GitHub
cat ~/.ssh/id_ed25519.pub
# Копируем содержимое и добавляем на github.com/settings/keys

# 3. Меняем remote URL
git remote set-url origin git@github.com:username/maincomby_bot.git

# Теперь push работает без пароля:
git push origin main
# ✅ Авторизация через SSH ключ автоматически
```

---

## ⚡ Полный workflow после настройки автодеплоя

### Шаг 1: Сделал изменения локально

```bash
# Отредактировал файлы в PyCharm/VSCode/etc
nano app/main.py
```

### Шаг 2: Коммит и push в GitHub

```bash
git add .
git commit -m "feat: добавил новую функцию"
git push origin main
# ✅ Пушится в GitHub (HTTPS или SSH к GitHub)
```

### Шаг 3: Жди 1-5 минут

```bash
# Автоматически на сервере vdska.ru:
# 1. Cron скрипт проверяет GitHub каждые 5 минут
# 2. Если есть новые коммиты → git pull
# 3. Перезапускает Docker контейнер
# 4. Бот обновлён!

# ИЛИ через Webhook (если настроен):
# 1. GitHub отправляет webhook сразу после push
# 2. Сервер получает уведомление
# 3. git pull + restart (через ~30 секунд)
```

### Шаг 4: Проверка (опционально)

```bash
# Если хочешь проверить логи (через SSH к серверу):
ssh root@104.253.1.54 "tail -f /var/log/bot_autoupdate.log"

# Или через webhook:
ssh root@104.253.1.54 "tail -f /var/log/bot_deploy.log"
```

**НО:** SSH к серверу нужен только для просмотра логов, **НЕ для деплоя!**

---

## 📋 Частые вопросы

### Q: Нужен ли SSH для git push?

**A:** Да, но **SSH к GitHub**, а не к vdska.ru. Альтернатива - HTTPS с токеном.

### Q: Можно вообще без SSH?

**A:** Да! Используй HTTPS для GitHub:

```bash
git remote set-url origin https://github.com/username/maincomby_bot.git
git push origin main
# Username + Personal Access Token
```

### Q: Как получить GitHub Personal Access Token?

**A:**
1. Открой https://github.com/settings/tokens
2. **Generate new token** → **Classic**
3. Выбери права: `repo` (Full control of private repositories)
4. Скопируй токен (вида `ghp_xxxxxxxxxxxx`)
5. Используй вместо пароля при git push

### Q: А для vdska.ru SSH больше не нужен?

**A:** Нужен **только 1 раз** для настройки автодеплоя (5-15 минут). После этого все обновления через `git push`.

---

## ✅ Итого

### SSH к серверу vdska.ru:
- ❌ НЕ нужен для каждого обновления
- ✅ Нужен 1 раз для настройки автодеплоя
- ✅ Опционально для просмотра логов

### Git push в GitHub:
- ✅ Через **HTTPS + токен** (проще)
- ✅ Или через **SSH к GitHub** (безопаснее)
- ✅ **НЕ требует** подключения к vdska.ru

### Обновление бота:
```bash
git push origin main
# ↓
# GitHub обновлён
# ↓
# vdska.ru автоматически подтянет изменения через 1-5 минут
# ↓
# Бот перезапущен с новым кодом
# ✅ Готово!
```

**БЕЗ ПОДКЛЮЧЕНИЯ К vdska.ru!** 🎉

---

## 📚 См. также

- [QUICK_DEPLOY_GUIDE.md](./QUICK_DEPLOY_GUIDE.md) - Настройка автодеплоя за 5 минут
- [WEBHOOK_SETUP.md](./WEBHOOK_SETUP.md) - Webhook для быстрого деплоя
- [HOSTING_INFO.md](./HOSTING_INFO.md) - Информация о vdska.ru
