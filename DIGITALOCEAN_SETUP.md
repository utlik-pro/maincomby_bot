# 🚀 Настройка Telegram бота на DigitalOcean

Пошаговое руководство по развертыванию бота через интерфейс DigitalOcean.

---

## 📋 Варианты деплоя

У вас есть два основных варианта:

1. **DigitalOcean App Platform** (рекомендуется) - управляемый сервис, простой деплой
2. **DigitalOcean Droplet + Docker** - больше контроля, ручная настройка

---

## 🌟 Вариант 1: DigitalOcean App Platform (Рекомендуется)

### Преимущества:
- ✅ Автоматический деплой из GitHub
- ✅ Автоматическое масштабирование
- ✅ Встроенные SSL сертификаты
- ✅ Управление через веб-интерфейс
- ✅ Автоматические бэкапы

### Шаг 1: Подготовка GitHub репозитория

1. Убедитесь, что ваш код находится в GitHub репозитории
2. Репозиторий должен быть **публичным** или вы должны иметь доступ через GitHub App

### Шаг 2: Создание App в DigitalOcean

1. Войдите в [DigitalOcean Cloud](https://cloud.digitalocean.com)
2. В левом меню выберите **"Apps"**
3. Нажмите **"Create App"**

### Шаг 3: Подключение GitHub репозитория

1. Выберите **"GitHub"** как источник кода
2. Авторизуйтесь через GitHub, если потребуется
3. Выберите ваш репозиторий: `maincomby_bot`
4. Выберите ветку: `main` (или `master`)
5. Нажмите **"Next"**

### Шаг 4: Настройка компонента

DigitalOcean автоматически определит Docker приложение.

**Настройте следующие параметры:**

1. **Component Name:** `maincomby-bot`
2. **Build Command:** (оставьте пустым, Dockerfile сделает всё)
3. **Run Command:** (оставьте пустым, используется CMD из Dockerfile)
4. **HTTP Port:** (оставьте пустым, бот не использует HTTP порты)
5. **Health Check:** (отключите, т.к. бот не использует HTTP)

### Шаг 5: Настройка переменных окружения

Нажмите на **"Environment Variables"** и добавьте следующие переменные:

#### Обязательные переменные:

```bash
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=123456789,987654321
DATABASE_URL=sqlite+aiosqlite:///./data/bot.db
```

#### Опциональные переменные:

```bash
# OpenAI (для генерации контента)
OPENAI_API_KEY=ваш_ключ_openai
OPENAI_MODEL=gpt-5-chat-latest

# Часовой пояс
TIMEZONE=Europe/Minsk

# RSS фиды для новостей (через запятую)
NEWS_RSS_FEEDS=https://habr.com/ru/rss/best/daily/,https://tproger.ru/rss

# Модерация новостей
INTERMEDIATE_CHAT_ID=-1001234567890
TARGET_CHANNEL_ID=-1001234567890

# Приветствия
WELCOME_CHAT_ID=-1001234567890
WELCOME_THREAD_ID=12345
```

**Важно:** Для каждой переменной нажмите **"Add Variable"** отдельно.

### Шаг 6: Настройка Volume (Persistent Storage)

База данных должна сохраняться между перезапусками:

1. В разделе **"Resources"** → **"Volumes"**
2. Нажмите **"Add Volume"**
3. Настройте:
   - **Name:** `bot-database`
   - **Mount Path:** `/app/data`
   - **Size:** 1 GB (достаточно для SQLite)
4. Нажмите **"Add"**

### Шаг 7: Выбор региона и плана

1. **Region:** Выберите ближайший регион (например, `Frankfurt` или `Amsterdam` для Европы)
2. **Plan:** Выберите **"Basic"** → **"Basic App"** → **"Basic - $5/month"**
   - Это минимальный план, достаточный для Telegram бота

### Шаг 8: Настройка автоматического деплоя

1. В разделе **"Deploy"** включите:
   - ✅ **"Auto Deploy on Push"** - автоматический деплой при push в main
   - ✅ **"Autoscaling"** (опционально) - если нужно масштабирование

2. Нажмите **"Next"**

### Шаг 9: Деплой

1. Проверьте все настройки
2. Нажмите **"Create Resources"**
3. DigitalOcean начнет создавать приложение (займет 2-5 минут)

### Шаг 10: Проверка

1. Дождитесь завершения деплоя (индикатор станет зеленым)
2. Откройте вкладку **"Runtime Logs"** - должны увидеть:
   ```
   Starting bot long-polling…
   ```
3. Проверьте бота в Telegram - отправьте `/start`

---

## 🔧 Вариант 2: DigitalOcean Droplet + Docker

### Шаг 1: Создание Droplet

1. В DigitalOcean выберите **"Droplets"** → **"Create Droplet"**
2. Настройте:
   - **Image:** `Docker` (Ubuntu с Docker)
   - **Plan:** `Basic` → `Regular` → `$6/month` (1 GB RAM, достаточный для бота)
   - **Region:** Выберите ближайший
   - **Authentication:** SSH ключ или пароль
3. Нажмите **"Create Droplet"**

### Шаг 2: Подключение к Droplet

После создания получите IP адрес и подключитесь:

```bash
ssh root@ваш_ip_адрес
```

### Шаг 3: Установка зависимостей

```bash
# Обновление системы
apt update && apt upgrade -y

# Docker уже установлен, но убедимся
docker --version
docker compose version

# Установка git (если нет)
apt install -y git
```

### Шаг 4: Клонирование репозитория

```bash
cd /root
git clone https://github.com/ваш-username/maincomby_bot.git
cd maincomby_bot
```

### Шаг 5: Создание .env файла

```bash
nano .env
```

Добавьте все необходимые переменные (см. Вариант 1, Шаг 5).

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 6: Создание директории для БД

```bash
mkdir -p data
chmod 755 data
```

### Шаг 7: Запуск бота через Docker Compose

```bash
docker compose up -d --build
```

### Шаг 8: Проверка

```bash
# Проверить статус
docker ps

# Посмотреть логи
docker logs maincomby_bot --tail 50

# Логи в реальном времени
docker logs -f maincomby_bot
```

### Шаг 9: Настройка автоматического запуска

Бот должен запускаться автоматически при перезагрузке сервера. Docker Compose с `restart: unless-stopped` уже настроен в `docker-compose.yml`.

---

## 🔄 Обновление бота

### Для App Platform:

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Обновление бота"
   git push origin main
   ```
3. DigitalOcean автоматически обнаружит изменения и перезапустит приложение

### Для Droplet:

```bash
# На сервере
cd /root/maincomby_bot
git pull origin main
docker compose up -d --build
```

---

## 📊 Мониторинг и логи

### App Platform:

1. В DigitalOcean: **Apps** → ваш бот → **Runtime Logs**
2. Или через CLI:
   ```bash
   doctl apps logs ваше_приложение_id
   ```

### Droplet:

```bash
# Логи в реальном времени
docker logs -f maincomby_bot

# Последние 100 строк
docker logs maincomby_bot --tail 100
```

---

## 💾 Резервное копирование

### Для App Platform:

1. Используйте встроенные снапшоты DigitalOcean
2. Или создайте скрипт для экспорта БД

### Для Droplet:

Создайте скрипт бэкапа:

```bash
nano /root/maincomby_bot/backup.sh
```

```bash
#!/bin/bash
BACKUP_DIR="/root/maincomby_bot/backups"
mkdir -p "$BACKUP_DIR"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
docker exec maincomby_bot sqlite3 /app/data/bot.db ".backup '/app/data/backup_${TIMESTAMP}.db'"
docker cp maincomby_bot:/app/data/backup_${TIMESTAMP}.db "$BACKUP_DIR/"
echo "Backup created: $BACKUP_DIR/backup_${TIMESTAMP}.db"
```

Сделайте скрипт исполняемым:

```bash
chmod +x /root/maincomby_bot/backup.sh
```

Настройте cron для автоматических бэкапов:

```bash
crontab -e
```

Добавьте строку (бэкап каждый день в 3:00):

```
0 3 * * * /root/maincomby_bot/backup.sh >> /var/log/bot_backup.log 2>&1
```

---

## 🔐 Безопасность

1. **Никогда не коммитьте .env файл** в Git
2. **Используйте переменные окружения** в DigitalOcean
3. **Регулярно обновляйте** зависимости
4. **Делайте бэкапы** базы данных
5. **Используйте SSH ключи** вместо паролей для Droplet

---

## 🐛 Решение проблем

### Бот не запускается

1. Проверьте логи в DigitalOcean или через `docker logs`
2. Убедитесь, что все переменные окружения заданы
3. Проверьте, что `BOT_TOKEN` корректный

### База данных не сохраняется

1. Убедитесь, что Volume настроен правильно (для App Platform)
2. Проверьте, что директория `/app/data` существует и доступна для записи
3. Для Droplet проверьте монтирование volume в `docker-compose.yml`

### Конфликт портов

Telegram бот не использует порты - только исходящие соединения к Telegram API.

### Проблемы с памятью

Если бот падает из-за нехватки памяти:
- Увеличьте план App Platform
- Или увеличьте размер Droplet

---

## 📝 Полезные команды

### Для App Platform:

```bash
# Через веб-интерфейс:
# Apps → ваш бот → Settings → Components → Edit

# Через CLI (установите doctl):
doctl apps get ваше_приложение_id
doctl apps logs ваше_приложение_id --follow
```

### Для Droplet:

```bash
# Остановить бота
docker compose down

# Запустить бота
docker compose up -d

# Перезапустить бота
docker compose restart

# Посмотреть статистику
docker stats maincomby_bot

# Подключиться к контейнеру
docker exec -it maincomby_bot sh
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в DigitalOcean
2. Проверьте документацию DigitalOcean
3. Проверьте статус сервисов Telegram API

---

## ✅ Чек-лист после настройки

- [ ] Бот запущен и отвечает в Telegram
- [ ] Команда `/start` работает
- [ ] Логи показывают `Starting bot long-polling…`
- [ ] База данных сохраняется между перезапусками
- [ ] Настроены автоматические бэкапы (для Droplet)
- [ ] Настроен автоматический деплой из GitHub (для App Platform)

---

## 🎉 Готово!

Ваш бот теперь работает на DigitalOcean! 🚀




