# Управление ботом на VPS

## ⚠️ КРИТИЧЕСКИ ВАЖНО! ⚠️

### 🚫 ЗАПРЕЩЕНО:
1. **НЕ УДАЛЯЙТЕ** директорию `/root/maincomby_bot` или любые файлы в ней!
2. **НЕ ИЗМЕНЯЙТЕ** файлы напрямую на сервере без бэкапа!
3. **НЕ УДАЛЯЙТЕ** базу данных `/root/maincomby_bot/data/bot.db`!
4. **НЕ УДАЛЯЙТЕ** директорию с бэкапами `/root/maincomby_bot/backups/`!
5. **НЕ ОСТАНАВЛИВАЙТЕ** cron задачу автоматического бэкапа!

### ✅ ВСЕГДА:
1. **СОЗДАВАЙТЕ БЭКАП** перед любыми изменениями: `/root/maincomby_bot/backup.sh`
2. **ИСПОЛЬЗУЙТЕ СКРИПТ** обновления: `/root/maincomby_bot/update.sh`
3. **ПРОВЕРЯЙТЕ ЛОГИ** после любых операций: `docker logs maincomby_bot`
4. **ТЕСТИРУЙТЕ** изменения локально перед развертыванием на продакшн

### 💾 В случае критической ситуации:
Все бэкапы хранятся в `/root/maincomby_bot/backups/` - используйте их для восстановления!

---

## Информация о сервере

- **IP:** 104.253.1.54
- **Логин:** root
- **Пароль:** gxorqwTvKEKv7
- **Расположение бота:** /root/maincomby_bot
- **База данных:** /root/maincomby_bot/data/bot.db

## Быстрый доступ

```bash
# Подключение к серверу
ssh root@104.253.1.54

# Или с паролем через sshpass
sshpass -p 'gxorqwTvKEKv7' ssh root@104.253.1.54
```

## Управление ботом

### Проверка статуса

```bash
# Список всех контейнеров
docker ps -a

# Статус бота
docker ps | grep maincomby

# Логи бота (последние 50 строк)
docker logs maincomby_bot --tail 50

# Логи в реальном времени
docker logs -f maincomby_bot
```

### Запуск и остановка

```bash
cd /root/maincomby_bot

# Остановить бота
docker stop maincomby_bot
# или
docker compose down

# Запустить бота
docker start maincomby_bot
# или
docker compose up -d

# Перезапустить бота
docker restart maincomby_bot
```

## Обновление бота

### Способ 1: Автоматический (рекомендуется)

На локальной машине:

```bash
# 1. Создать архив с обновлениями
tar czf /tmp/maincomby_bot_update.tar.gz app/ Dockerfile docker-compose.yml requirements.txt migrations/ .dockerignore

# 2. Загрузить на сервер
sshpass -p 'gxorqwTvKEKv7' scp /tmp/maincomby_bot_update.tar.gz root@104.253.1.54:/root/

# 3. Запустить скрипт обновления на сервере
sshpass -p 'gxorqwTvKEKv7' ssh root@104.253.1.54 "/root/maincomby_bot/update.sh"
```

### Способ 2: Ручной

```bash
ssh root@104.253.1.54

cd /root/maincomby_bot

# Создать бэкап
./backup.sh

# Остановить бота
docker compose down

# Распаковать обновления
tar xzf /root/maincomby_bot_update.tar.gz

# Пересобрать и запустить
docker compose up -d --build

# Проверить логи
docker logs maincomby_bot --tail 20
```

## Резервное копирование

### Автоматические бэкапы

Настроены автоматические ежедневные бэкапы в 3:00 UTC:

```bash
# Проверить задачи cron
crontab -l

# Просмотр логов бэкапов
cat /var/log/maincomby_bot_backup.log
```

### Ручной бэкап

```bash
# Запустить бэкап вручную
/root/maincomby_bot/backup.sh

# Просмотр всех бэкапов
ls -lh /root/maincomby_bot/backups/
```

### Восстановление из бэкапа

```bash
cd /root/maincomby_bot

# Остановить бота
docker stop maincomby_bot

# Восстановить базу из бэкапа
cp backups/bot_backup_YYYYMMDD_HHMMSS.db data/bot.db

# Запустить бота
docker start maincomby_bot

# Проверить логи
docker logs maincomby_bot --tail 20
```

## Работа с базой данных

### Просмотр данных

```bash
# Подключиться к серверу
ssh root@104.253.1.54

# Просмотр мероприятий
python3 -c "
import sqlite3
conn = sqlite3.connect('/root/maincomby_bot/data/bot.db')
cursor = conn.cursor()
cursor.execute('SELECT id, title, city, event_date, is_active FROM events')
for row in cursor.fetchall():
    print(f'ID: {row[0]}, {row[1]}, {row[2]}, {row[3]}, Active: {row[4]}')
conn.close()
"

# Просмотр регистраций
python3 -c "
import sqlite3
conn = sqlite3.connect('/root/maincomby_bot/data/bot.db')
cursor = conn.cursor()
cursor.execute('SELECT COUNT(*) FROM event_registrations')
print(f'Всего регистраций: {cursor.fetchone()[0]}')
conn.close()
"
```

### Экспорт базы данных

```bash
# Скачать БД на локальную машину
sshpass -p 'gxorqwTvKEKv7' scp root@104.253.1.54:/root/maincomby_bot/data/bot.db ./bot_backup.db
```

## Мониторинг

### Использование ресурсов

```bash
# Статистика контейнера
docker stats maincomby_bot --no-stream

# Размер контейнера
docker ps -s | grep maincomby

# Использование диска
df -h /root/maincomby_bot
```

### Проверка работоспособности

```bash
# Проверить, что бот запущен
docker ps | grep maincomby

# Последние логи
docker logs maincomby_bot --tail 20

# Проверить доступность БД
ls -lh /root/maincomby_bot/data/bot.db
```

## Очистка

### Удаление старых образов

```bash
# Удалить неиспользуемые образы
docker image prune -a

# Удалить неиспользуемые контейнеры
docker container prune
```

### Очистка старых бэкапов

```bash
# Бэкапы старше 30 дней удаляются автоматически
# Для ручного удаления:
find /root/maincomby_bot/backups -name 'bot_backup_*.db' -type f -mtime +30 -delete
```

## Миграции базы данных

При добавлении новых полей в БД:

```bash
cd /root/maincomby_bot

# Остановить бота
docker stop maincomby_bot

# Создать бэкап
./backup.sh

# Применить миграцию
python3 migrations/название_миграции.py

# Запустить бота
docker start maincomby_bot
```

## Полезные команды

### Просмотр переменных окружения

```bash
# В контейнере
docker exec maincomby_bot env | grep BOT_TOKEN
```

### Выполнение команд в контейнере

```bash
# Открыть shell в контейнере
docker exec -it maincomby_bot sh

# Выполнить команду
docker exec maincomby_bot ls -la /app/data
```

### Перезапуск всех сервисов

```bash
# Полная перезагрузка Docker
systemctl restart docker

# Перезапуск бота после перезагрузки Docker
cd /root/maincomby_bot
docker compose up -d
```

## Troubleshooting

### Бот не отвечает

```bash
# 1. Проверить статус контейнера
docker ps -a | grep maincomby

# 2. Проверить логи на ошибки
docker logs maincomby_bot --tail 100

# 3. Перезапустить бота
docker restart maincomby_bot
```

### База данных повреждена

```bash
# 1. Остановить бота
docker stop maincomby_bot

# 2. Проверить целостность БД
python3 -c "
import sqlite3
conn = sqlite3.connect('/root/maincomby_bot/data/bot.db')
conn.execute('PRAGMA integrity_check')
print(conn.fetchone())
conn.close()
"

# 3. Восстановить из последнего бэкапа
ls -lt /root/maincomby_bot/backups/ | head -5
cp /root/maincomby_bot/backups/bot_backup_LATEST.db /root/maincomby_bot/data/bot.db

# 4. Запустить бота
docker start maincomby_bot
```

### Нет места на диске

```bash
# Проверить использование диска
df -h

# Удалить старые Docker образы
docker system prune -a

# Удалить старые логи
journalctl --vacuum-time=7d
```

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи: `docker logs maincomby_bot --tail 100`
2. Создайте бэкап перед любыми изменениями
3. Обратитесь к документации

**Важно:** Всегда делайте бэкап перед обновлениями и изменениями!
