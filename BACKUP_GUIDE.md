# 💾 Руководство по бэкапу всех проектов

## ✅ Что установлено

На вашем сервере создан скрипт автоматического бэкапа: `/root/full_backup.sh`

### Что бэкапится:

1. **maincomby_bot** - ваш Telegram бот
2. **security_logs** - логи безопасности
3. **Docker volumes** - данные всех Docker контейнеров
4. **Базы данных** - PostgreSQL, Redis и другие
5. **Конфигурации** - все `.env` и `docker-compose.yml` файлы

---

## 🚀 Как сделать бэкап

### Способ 1: Автоматический бэкап через SSH

На своём Mac выполните:

```bash
ssh root@104.253.1.54 "bash /root/full_backup.sh"
```

Пароль: `gxorqwTvKEKv7`

### Способ 2: Через VNC консоль

1. Войдите в VNC консоль Proxmox
2. Выполните:
```bash
bash /root/full_backup.sh
```

---

## 📥 Как скачать бэкап

### После завершения бэкапа:

Скрипт покажет команду для скачивания. Обычно это:

```bash
scp root@104.253.1.54:/root/backups/full_backup_ДАТА.tar.gz ~/Desktop/
```

Например:
```bash
scp root@104.253.1.54:/root/backups/full_backup_2025-11-22_11-15-30.tar.gz ~/Desktop/
```

### Скачать последний бэкап:

```bash
LAST_BACKUP=$(ssh root@104.253.1.54 "ls -t /root/backups/full_backup_*.tar.gz | head -1")
scp root@104.253.1.54:$LAST_BACKUP ~/Desktop/
```

---

## 📊 Что включает бэкап

### 1. Проекты (/root/)
- `maincomby_bot/` - Telegram бот со всеми файлами
- `security_logs/` - Логи безопасности

### 2. Docker Volumes
Все данные Docker контейнеров:
- code-twenty (PostgreSQL, Redis, Server, Worker)
- dokploy (Система деплоя)
- maincomby_bot

### 3. Базы данных
Экспорт всех PostgreSQL баз данных в SQL формате

### 4. Конфигурации
- Все `docker-compose.yml` файлы
- Все `.env` файлы с секретами

---

## 🔄 Восстановление из бэкапа

### На новом сервере:

```bash
# 1. Загрузите бэкап на новый сервер
scp ~/Desktop/full_backup_*.tar.gz root@НОВЫЙ_IP:/root/

# 2. Распакуйте
ssh root@НОВЫЙ_IP
cd /root
tar -xzf full_backup_*.tar.gz
cd full_backup_*/

# 3. Восстановите maincomby_bot
cp -r maincomby_bot/ /root/
cd /root/maincomby_bot
docker compose up -d --build

# 4. Восстановите базу данных (если нужно)
docker exec -i maincomby_bot python3 -c "..." < databases/*.sql
```

---

## ⏰ Автоматический бэкап

### Настроить еженедельный бэкап:

```bash
ssh root@104.253.1.54
crontab -e
```

Добавьте строку:
```cron
0 4 * * 0 /root/full_backup.sh >> /var/log/backup.log 2>&1
```

Это запустит бэкап каждое воскресенье в 04:00.

---

## 🗑️ Управление бэкапами

### Посмотреть все бэкапы:

```bash
ssh root@104.253.1.54 "ls -lh /root/backups/"
```

### Удалить старые бэкапы (вручную):

```bash
ssh root@104.253.1.54 "find /root/backups -name 'full_backup_*.tar.gz' -mtime +30 -delete"
```

Это удалит бэкапы старше 30 дней.

### Автоматическая очистка:

Скрипт автоматически удаляет бэкапы старше 7 дней при каждом запуске.

---

## 📋 Проверка бэкапа

### Размер последнего бэкапа:

```bash
ssh root@104.253.1.54 "du -sh /root/backups/full_backup_*.tar.gz | tail -1"
```

### Содержимое бэкапа:

```bash
ssh root@104.253.1.54 "tar -tzf \$(ls -t /root/backups/full_backup_*.tar.gz | head -1) | head -20"
```

---

## 🚨 Экстренный бэкап перед блокировкой

Если получили предупреждение о блокировке:

### 1. Быстрый бэкап (5 минут)

```bash
ssh root@104.253.1.54 "bash /root/full_backup.sh"
```

### 2. Скачайте сразу после завершения

```bash
LAST=$(ssh root@104.253.1.54 "ls -t /root/backups/full_backup_*.tar.gz | head -1")
scp root@104.253.1.54:$LAST ~/Desktop/emergency_backup_$(date +%Y-%m-%d).tar.gz
```

### 3. Проверьте размер

```bash
ls -lh ~/Desktop/emergency_backup_*.tar.gz
```

Если размер > 100MB - бэкап успешен.

---

## 📦 Что делать с бэкапом

### Рекомендуемое хранение:

1. **Локально на Mac**: `~/Documents/VPS_Backups/`
2. **В облаке**:
   - Google Drive
   - Dropbox
   - iCloud Drive

### Структура хранения:

```
~/Documents/VPS_Backups/
├── 2025-11/
│   ├── full_backup_2025-11-22.tar.gz
│   ├── full_backup_2025-11-15.tar.gz
│   └── full_backup_2025-11-08.tar.gz
├── 2025-12/
│   └── ...
└── README.txt  # Описание содержимого
```

---

## 🔐 Безопасность бэкапов

### ⚠️ ВАЖНО:

Бэкапы содержат:
- Пароли к базам данных
- API ключи
- Токены ботов
- SSH ключи

### Рекомендации:

1. ✅ Храните бэкапы в безопасном месте
2. ✅ Шифруйте бэкапы перед загрузкой в облако
3. ✅ Не публикуйте бэкапы в открытом доступе
4. ✅ Удаляйте бэкапы с сервера после скачивания

### Шифрование бэкапа:

```bash
# Зашифровать
gpg -c ~/Desktop/full_backup_2025-11-22.tar.gz

# Расшифровать
gpg ~/Desktop/full_backup_2025-11-22.tar.gz.gpg
```

---

## 📞 Быстрые команды

### Полный цикл бэкапа:

```bash
# 1. Создать бэкап
ssh root@104.253.1.54 "bash /root/full_backup.sh"

# 2. Скачать последний
LAST=$(ssh root@104.253.1.54 "ls -t /root/backups/full_backup_*.tar.gz | head -1")
scp root@104.253.1.54:$LAST ~/Desktop/

# 3. Проверить
ls -lh ~/Desktop/full_backup_*.tar.gz
```

---

## ✅ Итого

### Автоматизировано:

✅ Полный бэкап всех проектов
✅ Экспорт баз данных
✅ Сохранение конфигураций
✅ Очистка старых бэкапов

### Ваша задача:

1. **Раз в неделю** - запускать бэкап и скачивать
2. **Перед изменениями** - делать бэкап
3. **При получении abuse** - экстренный бэкап

### Время выполнения:

- Создание бэкапа: ~2-5 минут
- Скачивание: ~1-2 минуты (зависит от размера)
- Восстановление: ~5-10 минут

---

**Дата создания:** 22.11.2025
**Скрипт:** `/root/full_backup.sh`
**Папка бэкапов:** `/root/backups/`
