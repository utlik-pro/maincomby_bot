# 🔐 Система защиты от банов - Полная инструкция

## ✅ Что установлено на сервере

На вашем сервере (104.253.1.54) создана система автоматического логирования и мониторинга:

### 📁 Расположение: `/root/security_logs/`

```
/root/security_logs/
├── README.md                    # Описание системы
├── collect_logs.sh              # Скрипт сбора всех логов
├── monitor_outgoing.sh          # Мониторинг исходящих подключений
├── network_connections/         # История сетевых подключений
├── docker_logs/                 # Логи всех Docker контейнеров
├── auth_logs/                   # Логи входов по SSH
├── audit/                       # Аудит системы (процессы, команды)
└── incidents/                   # Все инциденты и жалобы
```

---

## 🤖 Автоматическая работа

### Что происходит автоматически:

1. **Каждый день в 03:00** - полный сбор логов:
   - Все сетевые подключения
   - Логи всех Docker контейнеров
   - История SSH входов
   - Список процессов
   - Открытые порты
   - История команд

2. **Каждый час** - мониторинг подозрительной активности:
   - Проверка исходящих SSH подключений (порт 22)
   - Проверка подключений к базам данных
   - Сохранение алертов при обнаружении

3. **Автоматическое удаление** старых логов (старше 30 дней)

---

## 📥 Как скачать логи на свой компьютер

### Вариант 1: Скачать ВСЕ логи

Откройте терминал на своём Mac и выполните:

```bash
scp -r root@104.253.1.54:/root/security_logs ~/Desktop/security_logs_backup_$(date +%Y-%m-%d)
```

Введите пароль: `gxorqwTvKEKv7`

Логи будут сохранены на Рабочий стол в папке `security_logs_backup_ДАТА`

### Вариант 2: Скачать только инциденты

```bash
scp -r root@104.253.1.54:/root/security_logs/incidents ~/Desktop/incidents_$(date +%Y-%m-%d)
```

### Вариант 3: Скачать логи за конкретную дату

```bash
# Например, за 22 ноября 2025
scp root@104.253.1.54:/root/security_logs/network_connections/*2025-11-22* ~/Desktop/
```

---

## 🚨 Что делать при получении жалобы Abuse

### Шаг 1: Сохраните жалобу на сервер

```bash
ssh root@104.253.1.54
cd /root/security_logs/incidents
nano abuse_report_$(date +%Y-%m-%d).txt
```

Вставьте текст жалобы, нажмите `Ctrl+O` (сохранить), `Enter`, `Ctrl+X` (выйти)

### Шаг 2: Немедленно соберите все логи

```bash
/root/security_logs/collect_logs.sh
```

### Шаг 3: Скачайте логи на свой компьютер

На своём Mac выполните:

```bash
scp -r root@104.253.1.54:/root/security_logs ~/Desktop/evidence_$(date +%Y-%m-%d)
```

### Шаг 4: Отправьте в техподдержку

Напишите в тикет:

```
Здравствуйте!

Получили жалобу abuse. Провели расследование.

Доказательства нашей активности:
1. Логи всех Docker контейнеров за период
2. История сетевых подключений
3. Список процессов на сервере
4. SSH аутентификация

Все логи собраны и доступны в /root/security_logs/
При необходимости можем предоставить полный архив.

Предприняли меры по усилению безопасности.
```

---

## 🔍 Проверка системы прямо сейчас

### Проверить, что cron задачи работают:

```bash
ssh root@104.253.1.54 "crontab -l"
```

Должны быть строки:
```
0 3 * * * /root/security_logs/collect_logs.sh
0 * * * * /root/security_logs/monitor_outgoing.sh
```

### Посмотреть последние алерты:

```bash
ssh root@104.253.1.54 "cat /root/security_logs/incidents/alerts_*.log"
```

### Посмотреть активные подключения:

```bash
ssh root@104.253.1.54 "netstat -tunapl | grep ESTABLISHED"
```

---

## 📊 Ручной сбор логов

Если нужно собрать логи прямо сейчас (не дожидаясь 03:00):

```bash
ssh root@104.253.1.54
cd /root/security_logs
./collect_logs.sh
```

---

## 💾 Рекомендации по резервному копированию

### Еженедельное резервное копирование

**Каждую неделю** скачивайте логи на свой компьютер:

```bash
# В понедельник утром
scp -r root@104.253.1.54:/root/security_logs ~/Desktop/weekly_backup_$(date +%Y-%m-%d)
```

### Хранение логов

- Храните логи минимум **90 дней**
- Создайте папку на Mac: `~/Documents/VPS_Security_Logs/`
- Раскладывайте по месяцам: `2025-11/`, `2025-12/` и т.д.

---

## 📝 История инцидентов

### Инцидент 1: 21-22 ноября 2025

**Файл:** `/root/security_logs/incidents/2025-11-21_abuse_report.txt`

**Суть:** Блокировка сети из-за сканирования SSH портов

**Статус:** ✅ Разрешено, сеть восстановлена 22.11.2025

**Действия:**
- Создана система логирования
- Настроен автоматический мониторинг
- Логи собраны для доказательства

---

## 🛡️ Защита на будущее

### Что мониторится:

✅ Исходящие SSH подключения (порт 22)
✅ Подключения к базам данных
✅ Необычная активность Docker контейнеров
✅ История всех команд администратора
✅ Список всех процессов

### Что НЕ мониторится (безопасно):

✅ Входящие подключения к вашему боту
✅ Обычная работа Docker контейнеров
✅ HTTP/HTTPS трафик

---

## 📞 Быстрые команды

### Скачать всё за сегодня:

```bash
scp -r root@104.253.1.54:/root/security_logs ~/Desktop/logs_$(date +%Y-%m-%d)
```

### Посмотреть размер логов:

```bash
ssh root@104.253.1.54 "du -sh /root/security_logs/*"
```

### Проверить подозрительную активность:

```bash
ssh root@104.253.1.54 "/root/security_logs/monitor_outgoing.sh && cat /root/security_logs/incidents/alerts_*.log"
```

---

## ✅ Итого

Система полностью настроена и работает автоматически.

**Вам нужно только:**
1. Раз в неделю скачивать логи на свой компьютер
2. При получении жалобы - собрать логи и отправить в поддержку
3. Хранить логи минимум 90 дней

**Система защищает вас:**
- Собирает доказательства вашей активности
- Обнаруживает атаки до бана
- Сохраняет историю для расследований

---

**Дата создания:** 22.11.2025
**Статус:** ✅ Активна и работает
**Локация логов:** `/root/security_logs/`
