# Руководство по использованию новых функций бота

## 📋 Обзор новых функций

### 1. **Рассылка видео-кружков** (Broadcast)
Отправка видео-сообщений участникам для важных анонсов и напоминаний.

### 2. **Версионирование регистраций** (Old/New Date)
Система отслеживания регистраций до и после изменения даты мероприятия.

### 3. **Система подтверждения участия** (Confirmation)
Запрос подтверждения от участников при изменении даты/места мероприятия.

### 4. **Расширенная статистика** (Enhanced Statistics)
Детальная аналитика по городам, версиям регистрации и динамике подтверждений.

---

## 🚀 Начало работы

### Шаг 1: Применение миграции БД

**На VPS сервере:**
```bash
# Остановить бота
ssh root@104.253.1.54
cd /root/maincomby_bot
docker stop maincomby_bot

# Применить миграцию
python3 migrations/add_registration_versioning.py

# Запустить бота
docker start maincomby_bot
```

**Локально:**
```bash
cd /Users/admin/maincomby_bot
python3 migrations/add_registration_versioning.py
```

### Шаг 2: Размещение видео файла

Видео-кружок должен находиться в корне проекта:
```
/Users/admin/maincomby_bot/7486356307190949804.MP4
```

Для VPS:
```bash
scp /Users/admin/maincomby_bot/7486356307190949804.MP4 root@104.253.1.54:/root/maincomby_bot/
```

---

## 📹 Рассылка видео-кружков

### Команда: `/broadcast_video_test`
**Назначение:** Тестовая рассылка только администраторам.

**Использование:**
```
/broadcast_video_test
```

**Результат:**
- Видео-кружок отправляется всем администраторам из `ADMIN_IDS`
- Отчет с количеством успешных/неудачных отправок
- Логирование в `broadcast.log`

---

### Команда: `/broadcast_video [event_id]`
**Назначение:** Рассылка видео всем участникам мероприятия.

**Примеры:**
```bash
# Отправить всем зарегистрированным участникам
/broadcast_video

# Отправить только участникам мероприятия с ID=1
/broadcast_video 1
```

**Особенности:**
- Anti-flood защита (0.05 сек между отправками)
- Отслеживание заблокировавших бота пользователей
- Детальный отчет по результатам

---

### Команда: `/broadcast_text <текст>`
**Назначение:** Рассылка текстового сообщения участникам.

**Примеры:**
```bash
# Отправить всем
/broadcast_text Напоминаем о предстоящем мероприятии!

# Отправить участникам события 1
/broadcast_text 1 Мероприятие начнется через час!
```

---

## 📅 Версионирование и подтверждение

### Сценарий: Изменение даты мероприятия

#### Шаг 1: Пометить старые регистрации
**Команда:** `/mark_old_registrations <event_id>`

```bash
/mark_old_registrations 1
```

**Что происходит:**
- Все текущие регистрации помечаются как `old_date`
- Поле `confirmed` устанавливается в `false`
- Новые регистрации после этого момента будут `new_date`

---

#### Шаг 2: Изменить дату мероприятия
Используйте стандартный процесс редактирования мероприятия (если есть) или через БД:

```sql
UPDATE events
SET event_date = '2025-02-15 19:00:00',
    location = 'Новое место'
WHERE id = 1;
```

---

#### Шаг 3: Запросить подтверждения
**Команда:** `/request_confirmation <event_id>`

```bash
/request_confirmation 1
```

**Что происходит:**
1. Бот отправляет сообщение каждому участнику со старой датой
2. Сообщение содержит:
   - Информацию о новой дате и месте
   - Inline кнопки: "✅ Подтверждаю" / "❌ Не смогу прийти"
3. Обновляется поле `confirmation_requested_at`
4. Админы получают уведомления о каждом ответе

**Пример сообщения участнику:**
```
⚠️ ВАЖНОЕ УВЕДОМЛЕНИЕ ⚠️

Дата и/или место проведения мероприятия изменились:

🍳 ИИшница #5

🏙 Город: Минск
🗓 Новая дата: 15.02.2025
🕙 Время: 19:00
📍 Место: Коворкинг "Новое пространство"

Пожалуйста, подтвердите ваше участие с новыми условиями:
• Если вы сможете прийти - нажмите '✅ Подтверждаю'
• Если не сможете - нажмите '❌ Не смогу прийти'

Ждём вас! 🎉
```

---

## 📊 Расширенная статистика

### Команда: `/event_stats <event_id>`
**Обновленная статистика мероприятия.**

```bash
/event_stats 1
```

**Показывает:**
- Общее количество регистраций
- **NEW:** Разделение по версиям (старая/новая дата)
- **NEW:** Статистика подтверждений:
  - ✅ Подтвердили
  - ⏳ Не ответили
  - ❌ Отказались
- **NEW:** Коэффициент конверсии подтверждений
- Список участников с контактами

**Пример вывода:**
```
📊 Статистика мероприятия:

ИИшница #5
🏙 Город: Минск
📅 Дата: 15.02.2025 в 19:00

📈 Общая статистика:
✅ Зарегистрировано: 45
❌ Отменено: 3
📋 Всего регистраций: 48

📅 По датам регистрации:
👴 Старая дата: 30 чел.
   ├─ ✅ Подтвердили: 22
   ├─ ⏳ Не ответили: 5
   └─ ❌ Отказались: 3
   📊 Конверсия: 73.3%

🆕 Новая дата: 15 чел.
```

---

### Команда: `/confirmation_stats <event_id>`
**Детальная статистика подтверждений.**

```bash
/confirmation_stats 1
```

**Показывает:**
- Общее количество участников со старой датой
- Сколько запросов отправлено
- Процент подтвердивших/отказавшихся/не ответивших
- **Список неответивших** с контактами (до 10 человек)

**Пример вывода:**
```
📊 Статистика подтверждений

Мероприятие: ИИшница #5
Город: Минск
Дата: 15.02.2025 в 19:00

📈 Итоги:
👥 Всего участников (старая дата): 30
📧 Отправлено запросов: 30

✅ Подтвердили: 22
❌ Отказались: 3
⏳ Не ответили: 5

📊 Процентное соотношение:
✅ Подтвердили: 73.3%
❌ Отказались: 10.0%
⏳ Не ответили: 16.7%

⏳ Не ответили (5 чел.):
1. Иван Петров (@ivan_p)
   📱 +375291234567
   📅 Запрос: 10.02 14:30
...
```

---

### Команда: `/registration_timeline <event_id>`
**Динамика регистраций по датам.**

```bash
/registration_timeline 1
```

**Показывает:**
- Количество регистраций по каждой дате
- Разделение на новую/старую дату (если применимо)

**Пример вывода:**
```
📅 Динамика регистраций

Мероприятие: ИИшница #5
Всего регистраций: 48

05.01.2025: 12 рег. (🆕 новая дата)
06.01.2025: 8 рег. (🆕 новая дата)
07.01.2025: 10 рег. (🆕 новая дата)
[Дата была изменена]
10.02.2025: 7 рег. (🆕5 / 👴2)
11.02.2025: 6 рег. (🆕 новая дата)
12.02.2025: 5 рег. (🆕 новая дата)
```

---

## 🔄 Типичные workflow

### Workflow 1: Тестирование рассылки видео
```bash
# 1. Проверить наличие видео файла
ls -lh /Users/admin/maincomby_bot/7486356307190949804.MP4

# 2. Запустить тестовую рассылку
/broadcast_video_test

# 3. Проверить получение видео всеми админами
# Должны получить все из ADMIN_IDS

# 4. Если всё ок, отправить участникам
/broadcast_video 1
```

---

### Workflow 2: Изменение даты мероприятия
```bash
# 1. Пометить существующие регистрации
/mark_old_registrations 1

# 2. Изменить дату мероприятия (через БД или админ-команды)

# 3. Запросить подтверждения
/request_confirmation 1

# 4. Мониторить отклики
/confirmation_stats 1

# 5. При необходимости повторить запрос неответившим
# (повторный /request_confirmation отправит только тем, кто не ответил)
```

---

### Workflow 3: Анализ мероприятия
```bash
# 1. Общая статистика
/event_stats 1

# 2. Детальная статистика подтверждений
/confirmation_stats 1

# 3. Динамика регистраций
/registration_timeline 1

# 4. Экспорт лидов в CSV
/export_leads 1
```

---

## 🛠 Технические детали

### Структура БД

**Новые поля в `event_registrations`:**
```sql
registration_version VARCHAR(16) DEFAULT 'new_date'  -- old_date / new_date
confirmed BOOLEAN DEFAULT FALSE                      -- Подтверждено ли
confirmation_requested_at DATETIME                   -- Когда запрошено
```

**Индексы:**
```sql
CREATE INDEX ix_event_registrations_version ON event_registrations(registration_version);
CREATE INDEX ix_event_registrations_confirmed ON event_registrations(confirmed);
```

---

### Callback Data Format

Inline кнопки используют следующий формат:

```
confirm_<event_id>_<user_tg_id>  # Подтверждение
decline_<event_id>_<user_tg_id>  # Отказ
```

**Пример:**
```
confirm_1_123456789   # Пользователь 123456789 подтверждает участие в событии 1
decline_1_123456789   # Пользователь 123456789 отказывается от события 1
```

---

### Anti-Flood Protection

Все массовые рассылки используют задержку:
```python
await asyncio.sleep(0.05)  # 50мс между отправками
```

**Telegram лимиты:**
- 30 сообщений/сек для ботов
- 0.05 сек = 20 сообщений/сек (безопасная зона)

---

### Логирование

**Broadcast логи:**
- Все рассылки логируются через `loguru`
- Успешные отправки: `logger.info()`
- Ошибки: `logger.error()`
- Заблокировавшие бота: `logger.warning()`

**Confirmation логи:**
- Запросы подтверждений: `logger.info()`
- Ответы участников: `logger.info()`
- Уведомления админам при каждом подтверждении/отказе

---

## ⚠️ Важные замечания

### 1. Видео-кружки
- **Формат:** MP4
- **Максимальная длительность:** 1 минута
- **Рекомендуемый размер:** до 5 МБ
- Файл должен быть в корне проекта

### 2. Подтверждения
- Запросы можно отправлять повторно
- Бот отправит только тем, кто еще не ответил
- Админы получают уведомление о каждом ответе

### 3. Версионирование
- Команду `/mark_old_registrations` нужно вызвать **ДО** изменения даты
- Новые регистрации автоматически получают `new_date`
- Старые регистрации **не изменяются** автоматически

### 4. Безопасность
- Все команды доступны только администраторам (`ADMIN_IDS`)
- Callback кнопки проверяют ID пользователя
- Нельзя подтвердить чужую регистрацию

---

## 🐛 Troubleshooting

### Проблема: Видео не отправляется
**Решение:**
```bash
# Проверить наличие файла
ls -lh /Users/admin/maincomby_bot/7486356307190949804.MP4

# Проверить права доступа
chmod 644 /Users/admin/maincomby_bot/7486356307190949804.MP4

# Проверить формат (должен быть MP4)
file /Users/admin/maincomby_bot/7486356307190949804.MP4
```

---

### Проблема: Inline кнопки не работают
**Причины:**
1. Callback handler не зарегистрирован
2. Формат callback_data неверный
3. Пользователь пытается подтвердить чужую регистрацию

**Проверка:**
```bash
# Проверить логи
docker logs maincomby_bot --tail 50

# Проверить callback pattern
grep "callback_confirm" app/handlers/events.py
```

---

### Проблема: Миграция не применилась
**Решение:**
```bash
# Проверить, существует ли БД
ls -lh bot.db

# Применить миграцию вручную
python3 migrations/add_registration_versioning.py

# Проверить структуру таблицы
sqlite3 bot.db "PRAGMA table_info(event_registrations);"
```

---

### Проблема: Админы не получают уведомления
**Причины:**
1. Неверный `ADMIN_IDS` в `.env`
2. Админ заблокировал бота

**Проверка:**
```bash
# Проверить ADMIN_IDS
cat .env | grep ADMIN_IDS

# Проверить логи
docker logs maincomby_bot | grep "Failed to notify admin"
```

---

## 📞 Контакты и поддержка

При возникновении вопросов:
1. Проверьте логи: `docker logs maincomby_bot --tail 100`
2. Проверьте статус БД: `sqlite3 bot.db ".tables"`
3. Проверьте применение миграции: `python3 migrations/add_registration_versioning.py`

---

## 🔄 Обновление на VPS

```bash
# 1. SSH на сервер
ssh root@104.253.1.54

# 2. Перейти в директорию
cd /root/maincomby_bot

# 3. Остановить бота
docker stop maincomby_bot

# 4. Скопировать файлы с локальной машины (с вашего Mac)
# На вашем Mac:
scp -r app/ root@104.253.1.54:/root/maincomby_bot/
scp migrations/add_registration_versioning.py root@104.253.1.54:/root/maincomby_bot/migrations/
scp 7486356307190949804.MP4 root@104.253.1.54:/root/maincomby_bot/

# 5. На VPS: применить миграцию
python3 migrations/add_registration_versioning.py

# 6. Пересобрать и запустить
docker compose up -d --build

# 7. Проверить логи
docker logs maincomby_bot --tail 50 -f
```

---

## ✅ Checklist перед запуском

- [ ] Миграция БД применена
- [ ] Видео файл размещен в корне проекта
- [ ] Новый код развернут
- [ ] Бот перезапущен
- [ ] Тестовая рассылка `/broadcast_video_test` успешна
- [ ] Все админы получили тестовое видео
- [ ] Inline кнопки отображаются корректно
- [ ] Callback handlers работают
- [ ] Статистика показывает новые поля
- [ ] Логи не содержат ошибок

---

**Дата создания:** 2025-11-21
**Версия:** 1.0
