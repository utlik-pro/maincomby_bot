# 🚀 Деплой на Render.com

## ⚡ Быстрый старт за 5 минут

Render.com - это как Vercel, но для приложений которые работают 24/7!

---

## 📋 Что вам нужно

1. **GitHub аккаунт** - для кода
2. **Render.com аккаунт** - [зарегистрироваться бесплатно](https://render.com)
3. **Telegram Bot Token** - получить от [@BotFather](https://t.me/BotFather)

---

## 🎯 Шаг 1: Загрузка кода на GitHub

```bash
# Если еще не создали репозиторий
git init
git add .
git commit -m "Initial commit - ready for Render"

# Создайте новый репозиторий на GitHub, затем:
git remote add origin https://github.com/ваш-username/maincomby_bot.git
git branch -M main
git push -u origin main
```

---

## 🚀 Шаг 2: Деплой на Render (через Blueprint)

### Вариант А: Автоматический деплой (рекомендуется)

1. **Откройте** [Render Dashboard](https://dashboard.render.com)

2. **New** → **Blueprint**

3. **Connect GitHub** → выберите ваш репозиторий `maincomby_bot`

4. **Render автоматически**:
   - Обнаружит `render.yaml`
   - Создаст PostgreSQL базу данных
   - Создаст сервис для бота
   - Настроит связь между ними

5. **Добавьте переменные окружения**:
   - В Blueprint Settings → Environment Variables:
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   ADMIN_IDS=ваш_telegram_id
   ```

6. **Apply** → бот начнет деплоиться!

---

### Вариант Б: Ручной деплой

#### 2.1. Создайте PostgreSQL базу

1. **New** → **PostgreSQL**
2. **Name:** `maincomby-db`
3. **Database:** `maincomby_bot`
4. **User:** `maincomby_user`
5. **Region:** Frankfurt (ближе к Беларуси)
6. **Plan:** Free
7. **Create Database**

#### 2.2. Создайте Web Service

1. **New** → **Web Service**
2. **Connect GitHub** → выберите репозиторий
3. **Name:** `maincomby-bot`
4. **Region:** Frankfurt
5. **Branch:** `main`
6. **Runtime:** Python 3
7. **Build Command:** `pip install -r requirements.txt`
8. **Start Command:** `python -m app.main`
9. **Plan:** Free

#### 2.3. Настройте переменные окружения

В разделе **Environment Variables** добавьте:

```bash
# Обязательные
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id,другой_admin_id

# База данных (автоматически из PostgreSQL)
DATABASE_URL=внутренняя_ссылка_на_БД

# Опциональные
OPENAI_API_KEY=ваш_ключ
OPENAI_MODEL=gpt-4o-mini
TIMEZONE=Europe/Minsk

# ID чатов (если используете)
INTERMEDIATE_CHAT_ID=-1001234567890
TARGET_CHANNEL_ID=-1001234567890
WELCOME_CHAT_ID=-1001234567890
WELCOME_THREAD_ID=12345
```

#### 2.4. Подключите базу данных

1. В настройках Web Service → **Environment**
2. Найдите `DATABASE_URL`
3. Выберите **Add from Database** → выберите `maincomby-db`
4. Render автоматически подставит правильный URL

---

## ✅ Шаг 3: Проверка

### 3.1. Проверьте логи

1. В Dashboard → ваш сервис → **Logs**
2. Должно быть: `Starting bot long-polling…`
3. Если есть ошибки - проверьте переменные окружения

### 3.2. Проверьте бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должно появиться приветствие с мероприятием! 🎉

---

## 🔄 Автоматические обновления

Render автоматически пересобирает бота при каждом `git push`:

```bash
# Внесите изменения
git add .
git commit -m "Update bot"
git push

# Render автоматически задеплоит обновление!
```

---

## 📊 Мониторинг

### Просмотр логов в реальном времени
```
Dashboard → maincomby-bot → Logs
```

### Проверка состояния базы данных
```
Dashboard → maincomby-db → Metrics
```

### Перезапуск бота
```
Dashboard → maincomby-bot → Manual Deploy → Deploy latest commit
```

---

## 💾 Резервное копирование БД

Render автоматически делает бэкапы PostgreSQL:
- **Free plan**: 7 дней истории
- **Paid plan**: 30 дней истории

### Ручной бэкап

```bash
# Установите PostgreSQL клиент локально
brew install postgresql  # macOS
# или sudo apt install postgresql-client  # Linux

# Получите DATABASE_URL из Render Dashboard
# В разделе базы данных → Connect → External Connection

# Сделайте дамп
pg_dump "ваш_DATABASE_URL" > backup.sql

# Восстановление
psql "ваш_DATABASE_URL" < backup.sql
```

---

## 🆓 Бесплатный план Render

### Что включено (Free Plan):

✅ **750 часов** работы в месяц (достаточно для 24/7)
✅ **PostgreSQL** 1GB бесплатно
✅ **Автодеплой** из GitHub
✅ **SSL сертификаты** автоматически
✅ **Логи** последние 7 дней

### Ограничения:

⚠️ **Засыпает** после 15 минут неактивности
⚠️ **Пробуждение** занимает ~30 секунд

**Решение:** Для Telegram бота это не проблема - бот сам поддерживает соединение с Telegram, не засыпает!

---

## 🎁 Бонус: Бесплатный апгрейд

Render дает **$50 кредитов** новым пользователям при добавлении карты (карта не списывается!)

**Paid план ($7/мес) дает:**
- Не засыпает никогда
- Больше ресурсов CPU/RAM
- 30 дней бэкапов БД

---

## 🐛 Решение проблем

### Бот не запускается

**Проверьте:**
1. Логи: `Dashboard → Logs`
2. Переменные окружения: `BOT_TOKEN` задан?
3. База данных: `DATABASE_URL` подключен?

### Ошибка подключения к БД

```
sqlalchemy.exc.OperationalError: connection failed
```

**Решение:**
1. Проверьте что PostgreSQL база создана
2. Проверьте `DATABASE_URL` в переменных
3. Убедитесь что `asyncpg` установлен (в `requirements.txt`)

### База данных переполнена

**Решение:**
1. Upgrade на Paid план ($7/мес)
2. Или переключитесь на внешнюю БД (Supabase бесплатно)

---

## 📈 Масштабирование

### Переход на Paid план

1. **Dashboard** → **Account Settings**
2. **Billing** → **Add Payment Method**
3. **Upgrade** ваш сервис → **Starter ($7/мес)**

**Преимущества:**
- Не засыпает
- 512 MB → 2 GB RAM
- Priority support

---

## 📞 Поддержка

- **Документация:** [render.com/docs](https://render.com/docs)
- **Community:** [community.render.com](https://community.render.com)
- **Status:** [status.render.com](https://status.render.com)

---

## 🎉 Готово!

Ваш бот теперь работает на Render.com:
- ✅ Бесплатно
- ✅ Автоматические обновления
- ✅ PostgreSQL база
- ✅ Логи и мониторинг
- ✅ SSL из коробки

**Попробуйте:** Отправьте `/start` вашему боту! 🚀
